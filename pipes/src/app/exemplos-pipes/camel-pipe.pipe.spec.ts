import { CamelPipePipe } from './camel-pipe.pipe';

describe('CamelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CamelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
